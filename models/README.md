# Models

Trained model weights for the generator (Phase 08) are **not committed**. Track them with
Git LFS or DVC and store the binaries in object storage / a model registry.

This folder holds pointers and model cards only.
