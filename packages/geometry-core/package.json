{
  "name": "@fpg/geometry-core",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared browser-side geometry helpers (mm units): snapping, bbox, area, setbacks.",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "test": "vitest run --passWithNoTests"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
